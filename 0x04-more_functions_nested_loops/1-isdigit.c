#include <stdio.h>
#include <ctype.h>
#include "main.h"

/**
 * _isdigit - alphabet code
 * @c: The character to print
 *
 * Return: 1 if true 0 if false
 */

int _isdigit(int c)

{
if (isdigit(c))
	return (1);
return (0);

}
